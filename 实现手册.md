# Bigboom 实现手册

Yarn 风格的《生活大爆炸》双语台词检索站。本文是后续 AI Coding 的行动手册：以当前仓库真实状态为准，一次只做一个 Slice。

视觉参考：顶栏搜索 + 封面卡片网格 + 时长角标 + 中英台词。学信息结构，不复制 Yarn 商标。

---

## 当前进度

| 步骤 | 内容 | 状态 |
| --- | --- | --- |
| 1 | 脚手架、gitignore、Cursor 规则 | 完成 |
| 2 | Supabase 表、RLS、`search_cues`、私有桶 | 完成 |
| 3 | ingest CLI：R2 原片 → ffmpeg → Supabase 短片 | 完成（S01E01 共 377 条 ready） |
| 4 | 搜索页：双语卡片网格、季筛选、签名封面 | 完成 |
| 5 | 片段详情播放 + 上下文 | 完成 |
| 6 | 分享卡片 + 7 天私有链接 | 完成 |
| 7 | Vercel 生产部署 | 未做 |

本地开发：仓库根目录 `pnpm dev`，打开 http://localhost:3000

---

## 硬约束

- 原片 MP4 / SRT 只放在 **私有 R2**（`raw/video`、`raw/subs`）。不进 Git，不进 Supabase Storage，网站不拿整集当播放源。
- 本机 `data/work` 只在切片时临时下载，切完即删。
- 衍生素材：封面 JPEG、最长 **4 秒** 的 360p 短片、分享 PNG。MVP 不做云端二次裁剪。
- `SUPABASE_SERVICE_ROLE_KEY` 与全部 `R2_*` 只出现在 `scripts/.env` 或服务端。禁止 `NEXT_PUBLIC_`。
- 分享页必须 `noindex`。禁止做可被搜索引擎收录的公网原片/切片库。
- 免费档只灌第 1 季衍生视觉；第 2 季及以后 ingest 需要 `--force`。

---

## 产品 MVP

**做：** 中英关键词搜索、Yarn 风格卡片、点进片段循环播放、双语卡片图、7 天过期私有分享链。

**不做：** 整集在线播、时间轴精剪、悬停自动播、用户社区、登录墙、多剧。

一次成功路径：搜词 → 出卡片 → 点开循环播放短片 → 复制分享链接或下载双语卡片。

---

## 代价架构

计算放在开发机（ffmpeg，一次性），状态放在云（按量，量要小），页面放在 Vercel。

```text
L1 本机 ingest     ffmpeg 临时下载 R2 原片，切封面/短片     代价 $0
L2a R2 原片        raw/video + raw/subs，必须私有           按存储计，无出站更合适
L2b Supabase 衍生  thumbs / clips / cards，私有 + 签名 URL  容量和流量瓶颈
L3  Postgres       台词、对象键、search_cues                全剧台词仅几十 MB
L4  Next Route     搜索、签发 1 小时签名 URL、写 shares       Vercel Hobby
L5  UI             网格只渲染 JPEG，点击再播 mp4             $0
```

不要同时把原片再存一份到 Supabase Storage。不要双写两套素材库。

---

## 仓库地图

```text
E:\Bigboom\
  apps/web/                 Next.js App Router（搜索 + 片段播放已通）
  packages/db/              Drizzle schema，与 SQL 对齐
  scripts/ingest/           只在本机跑的 CLI
  supabase/migrations/      SQL 源（0001_init.sql 已推到云端）
  data/work/                gitignore，临时目录
  实现手册.md                 本文件
  AGENTS.md                 Agent 短约束
  .cursor/rules/bigboom.mdc
```

关键模块：

| 路径 | 职责 |
| --- | --- |
| `scripts/ingest/cli.ts` | `--push-raw` / `--dry-run` / 切片入库 |
| `scripts/ingest/parseSrt.ts` | SRT；中文字幕去 ASS 标签、只留中文行 |
| `scripts/ingest/align.ts` | 时间重叠对齐，IoU &lt; 0.3 则单语 |
| `scripts/ingest/cut.ts` | ffmpeg 封面宽 480、短片 360p ≤4s |
| `scripts/ingest/r2.ts` | 私有 R2 上传/下载/列举 |
| `scripts/ingest/upload.ts` | 衍生文件进 Supabase Storage，cue upsert |
| `supabase/migrations/0001_init.sql` | 表、RLS、RPC、三个私有桶 |
| `apps/web/app/api/cues/[id]/route.ts` | 签发封面和短片 URL |
| `apps/web/app/c/[id]/page.tsx` | 循环播放、双语、前后句、本集其它片段、复制/下载 |
| `apps/web/app/api/shares/route.ts` | 写入 7 天 token，不提供列表 |
| `apps/web/app/s/[token]/page.tsx` | 只播这一条，`noindex`，过期失效 |

---

## 语料约定

R2 对象键（桶名默认 `bigboom`，必须私有）：

```text
raw/video/TBBT_S01E01.mp4
raw/subs/TBBT_S01E01.en.srt
raw/subs/TBBT_S01E01.zh.srt
```

衍生键：`tbbt/s01/e01/{cueId}.jpg|mp4`，分别在 Supabase 桶 `thumbs` / `clips`。

一行 cue 用同一个 `cue_id` 绑数据库和对象。表里只存 key，不存盘符路径。

ingest 流程：扫 R2 → 下载字幕（dry-run 只下字幕）→ 对齐过滤 → 下载 MP4 → ffmpeg → 上传衍生 → upsert → 删除 `data/work` 临时文件。

幂等：已 `media_status=ready` 的 `(episode_id, start_ms, end_ms)` 会跳过，除非 `--force`。

---

## 数据模型（L3）

- `shows`：`slug=tbbt`
- `episodes`：季、集、`source_label`（仅文件名）
- `cues`：中英文本、`align_status`、`thumb_key`/`clip_key`、`clip_duration_ms≤4000`、`media_status`、生成列 `search_en`
- `shares`：token、过期时间（表已建，写入逻辑未做）
- RPC：`search_cues(q text, p_season int)`。参数不能叫 `season`（与 RETURNS TABLE 列名冲突）
- RLS：匿名只读 `media_status=ready` 的 cue；Storage 无公开读，靠服务端签名 URL

中文检索用 `pg_trgm` + `ILIKE`，不要再加 Meilisearch。

---

## 前端

### 已实现：`/` 搜索

- 背景极浅灰 `#fafafa`；标题、搜索框、示例词 sticky 固定在顶部
- 大标题为剧名 *The Big Bang Theory*，下方衬一句 Sheldon 经典台词
- 搜索框居中，右侧「搜索」按钮；示例词：Sheldon / Howard / Penny / peanut / 花生 / 霍华德
- 无 `q`：随机 9 条；有 `q`：结果三列网格直接出现在搜索框下方
- 点卡片在当前页弹出播放层；左右键切换同一次搜索（或首页 9 条）里的其它片段
- `?q=&season=&lang=&play=`；`lang` 只影响加粗；卡片本身不 autoplay

### 已实现：`/c/[id]` 播放

- 签名 mp4，`autoPlay muted loop controls`（浏览器自动播放必须静音，可点控件开声）
- 大号双语台词、集数 + 时间码
- 同集前后各 2 句可跳转；底部本集其它 8 条
- 若 URL 带 `q`，左右切换同一关键词的其它命中
- 复制 7 天私有链接、下载双语 PNG

### 已实现：分享

- `/s/[token]`：只播这一条 +「去搜索更多」，无网格、无筛选
- 过期或错误 token 显示失效页；`robots` 禁止收录 `/s/` 与 `/api/`
- PNG 缓存在 Supabase `cards` 桶；同一 cue 未过期则复用 token

---

## 环境变量

复制 `.env.example`。

`apps/web/.env.local`（Next）：

```text
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
```

`scripts/.env`（ingest）：上述 Supabase 项 + 

```text
R2_ACCOUNT_ID=          # Cloudflare 32 位 Account ID，不是 token 名字
R2_ACCESS_KEY_ID=
R2_SECRET_ACCESS_KEY=
R2_BUCKET=bigboom
```

PowerShell 若禁止运行 `npx.ps1` / `pnpm.ps1`，改用 `npx.cmd`、`pnpm.cmd`。

---

## 常用命令

在仓库根目录 `E:\Bigboom` 执行：

```powershell
pnpm dev
pnpm db:push
pnpm ingest -- --push-raw --episode S01E01
pnpm ingest -- --episode S01E01 --dry-run
pnpm ingest -- --episode S01E01 --limit 5
pnpm ingest -- --episode S01E01
pnpm ingest -- --season 1 --force
```

`R2_ACCOUNT_ID` 在 Cloudflare Dashboard → R2 右侧复制。上传原片看到 `raw files are on R2` 即成功。

---

## 下一步 Slice（未做）

每次只开一刀。对 Agent 说明目标、允许改的文件、不要做的下一刀、验收标准。

### 部署

- Vercel 导入私有仓库，Framework Next.js，Root Directory 若 monorepo 则按 `apps/web` 配置
- 环境变量：`NEXT_PUBLIC_SUPABASE_*`、`SUPABASE_SERVICE_ROLE_KEY`（不要加 R2 到前端）
- 验收：生产搜索 → 播放 → 分享；mp4 域名是 `*.supabase.co` 不是 Vercel；用量在免费档内

---

## 版权与范围

可以：自有正版视频 + 合法字幕，私有 R2 + 私有/演示站点。

不可以：盗版片源、公开原片桶、无登录可被收录的全集切片站。
